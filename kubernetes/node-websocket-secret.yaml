apiVersion: v1
kind: Secret
metadata:
  name: node-websocket-secret
type: Opaque
data:
  # Replace this placeholder with your actual base64 encoded DATABASE_URL.
  # To encode: echo -n "postgresql://user:password@db-host:5432/db-name?schema=public" | base64
  DATABASE_URL: "YOUR_BASE64_ENCODED_DATABASE_URL_HERE" # Placeholder corrected